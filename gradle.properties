org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
# R8's newer resource-shrinking pipeline (AGP 8.12+) — smaller/faster release builds, requires
# isMinifyEnabled/isShrinkResources already true on the release buildType (they are, in app/build.gradle.kts).
android.r8.optimizedResourceShrinking=true
