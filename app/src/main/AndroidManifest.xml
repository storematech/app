<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".QuizMakerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizMaker">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.QuizMaker.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Shareable quiz link: https://app.yunolms.com/take-quiz/{shareId} (current) or
                 https://yunolms.com/take-quiz/{shareId} / https://quiz-maker.online/take-quiz/{shareId}
                 (older links/QR codes already shared before the domain switches — kept working,
                 not just the new one). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="app.yunolms.com"
                    android:pathPrefix="/take-quiz" />
                <data
                    android:scheme="https"
                    android:host="yunolms.com"
                    android:pathPrefix="/take-quiz" />
                <data
                    android:scheme="https"
                    android:host="quiz-maker.online"
                    android:pathPrefix="/take-quiz" />
            </intent-filter>

            <!-- Fallback custom-scheme deep link, works without domain verification -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="quizmaker"
                    android:host="take-quiz" />
            </intent-filter>
        </activity>

        <service
            android:name=".core.messaging.QuizFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Fallback if a message ever arrives with the "notification" payload type instead of
             our usual data-only one — QuizFcmService.onMessageReceived() handles the normal path. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="quiz_submissions" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_splash_icon" />

        <!-- Lets us hand generated PDFs (e.g. leaderboard exports) to other apps via a content:// URI. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
